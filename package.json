{
  "name": "memory-persistor",
  "version": "1.0.0",
  "description": "PostgreSQL-backed MCP memory server with thermal decay for Claude Code",
  "type": "module",
  "scripts": {
    "dev": "tsx src/mcp-server.ts",
    "build": "tsc",
    "test": "vitest run --exclude 'tests/integration/**'",
    "test:integration": "vitest run tests/integration/ --fileParallelism=false",
    "migrate": "echo \"Use: make migrate (applies drizzle/*.sql via psql). drizzle-kit generate is destructive here - drizzle/meta/ is gitignored.\" && exit 1",
    "seed": "tsx src/import.ts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/effecet/memory-persistor.git"
  },
  "author": "effece",
  "license": "MIT",
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.23.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/pg": "^8.21.0",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
