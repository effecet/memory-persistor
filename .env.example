# Local Docker Postgres (dev / fallback)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/memory_persistor
POSTGRES_PASSWORD=postgres

# Where memory markdown files are mirrored (optional file-sync feature).
MEMORY_PERSISTOR_DIR=/path/to/memory-persistor
CLAUDE_DIR=/path/to/.claude
